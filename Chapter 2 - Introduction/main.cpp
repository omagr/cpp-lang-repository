#include <iostream>
using namespace std;

int main()
{
    //  one-line comment.
    /*
        multi-line comment. 
    */

    /*  
        n-1: block commnet cant be nested.
        n-2: Use comment carefully, dont overdo it though.
    */

    std::cout << "maa ki choo, bete!" << std::endl; // we do need to put semicolon end of the statement  
    std::cout << "baap ki choo, bete!" << std::endl;
    std::cout << "bhen ki choo, bete!" << std::endl;
    std::cout << "bhai ki choo, bete!" << std::endl;
    return 0; // it sent the msg to os that program is either failled or success
}