#include <iostream> // we do not need to put semicolon end of the statement

// starting point of any cpp programm
int main()
{
    std::cout << "lode ki choo, bete!" << std::endl; // we do need to put semicolon end of the statement
    std::cout << "baap ki choo, bete!" << std::endl;
    std::cout << "bhen ki choo, bete!" << std::endl;
    std::cout << "bhai ki choo, bete!" << std::endl;
    return 0; // it sent the msg to os that program is either failled or success
}